# Todos API with FastAPI and PostgreSQL

**A simple FastAPI application for managing todos. Users can create, read, update, and delete todos with a title, description, priority, and completion status.**

## EZ start

1. Clone the repository
2. Navigate to the project directory
3. run `docker compose up --build`
4. Open your browser and go to `https://localhost:4444` to see the application in action.

> tip: go to `https://localhost:4000/docs` to access the API documentation.

## Certs

Cert keys are shared for the sake of the project. These files are solely for development and learning purposes and are not intended for production use. They should not be used in a production environment or outside of the development or learning context.
