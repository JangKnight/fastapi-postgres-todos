anthonysjhenry.com, www.anthonysjhenry.com {
    @blocked {
        path */.env* /*.env */config* */config.* *.php
    }
    
    handle @blocked {
        abort
    }
    
    handle /api/* {
        reverse_proxy server:4000
    }

    handle {
        reverse_proxy client:4444 
    }
}
